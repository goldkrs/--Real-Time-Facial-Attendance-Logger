# Core dependencies (webcam + GUI + training/inference)
numpy
opencv-python
face-recognition
imutils
Pillow
scikit-learn
tensorflow
Flask

# Raspberry Pi camera dependencies (Linux/RPi only)
picamera2; platform_system == "Linux"
picamera; platform_system == "Linux"
